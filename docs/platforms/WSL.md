# WSL Setup

This guide covers using Windows Subsystem for Linux for Lorien development.

## Tips
- Use polling file watcher for Streamlit (`fileWatcherType = "poll"`)
- Share DB path via Windows user directory or inside WSL home
- Verify Flutter tooling inside WSL for Linux builds

See also: `docs/DEVELOPMENT.md` for general setup.

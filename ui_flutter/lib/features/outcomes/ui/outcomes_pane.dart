import 'package:flutter/material.dart';

class OutcomesPane extends StatelessWidget {
  const OutcomesPane({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Outcomes (placeholder)', style: TextStyle(fontSize: 18)),
    );
  }
}

import 'package:flutter/material.dart';

class FlagsPane extends StatelessWidget {
  const FlagsPane({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Flags (placeholder)', style: TextStyle(fontSize: 18)),
    );
  }
}

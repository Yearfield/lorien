import 'package:flutter/material.dart';

class HomePane extends StatelessWidget {
  const HomePane({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Home / Workspace', style: TextStyle(fontSize: 18)),
    );
  }
}

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('shows Offline when connectivity none', (tester) async {
    // Provide provider override if needed; assert text 'Offline'
    expect(true, isTrue);
  });
}

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Base URL persists and loads on startup', () {
    expect(
        true, isTrue); // implement with SharedPreferences.setMockInitialValues
  });
}

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('LLM health non-200 hides Fill button (logic level)', () {
    // Pseudo: ensure UI respects llmApi.health() == false
    expect(true, isTrue);
  });
}

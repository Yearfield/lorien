# Core API dependencies - Production requirements
# Compile with: pip-compile requirements.in --resolver=backtracking --generate-hashes

# FastAPI + Uvicorn (known-good combination)
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data validation and serialization
pydantic==2.9.2
pydantic-settings==2.5.2

# Data processing
pandas==2.2.3

# Excel support
XlsxWriter==3.2.0
openpyxl==3.1.5

# File upload support
python-multipart==0.0.12

# HTTP client
httpx==0.27.2

# Streamlit adapter dependencies
streamlit==1.39.0
requests==2.32.3
tenacity==9.0.0

# Google Sheets integration
gspread==6.1.2
gspread-dataframe==4.0.0
google-auth==2.35.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0

# OpenTelemetry - Optional observability dependencies
# Install with: pip install opentelemetry-distro opentelemetry-exporter-otlp
# These are optional and only loaded if OTEL_EXPORTER_OTLP_ENDPOINT is set
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0
# opentelemetry-instrumentation-fastapi>=0.41b0
# opentelemetry-instrumentation-logging>=0.41b0
# opentelemetry-instrumentation-requests>=0.41b0
# opentelemetry-exporter-otlp-proto-grpc>=1.20.0
